import { call, delay, fork, put, select, takeLatest } from 'redux-saga/effects';

export function* userDataSaga(): any {
    try {
    } catch (error) {
    } finally {
    }
}

export function* watcherSaga() {
    yield takeLatest('', userDataSaga);
}
